name: "Market"
main: "${project_group}.market.Market"
version: "${project_version}"
extension:
  depend: []
git:
  provider: github
  owner: ${git_org_name}
  repository: ${git_org_repository}
